setup:
  addons:
    - plan: heroku-postgresql:mini
      as: DATABASE
build:
  docker:
    worker: Dockerfile
run:
  worker: 
    command:
      - /app/start.sh
    image: worker
  web:
    command:
      - node index.js
    image: worker